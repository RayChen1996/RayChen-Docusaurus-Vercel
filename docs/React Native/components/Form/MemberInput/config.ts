import type {Control} from 'react-hook-form';

import type {SchemaType} from '@/form/members/saveMember';

/** - 預設值 */
export const initial = {
  control: undefined as unknown as Control<SchemaType>,
};
